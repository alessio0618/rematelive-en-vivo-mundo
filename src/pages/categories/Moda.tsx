
import React from 'react';
import CategoryPageTemplate from '@/components/CategoryPageTemplate';
import { categoryData } from '@/data/categoryData';

const Moda = () => {
  return <CategoryPageTemplate categoryData={categoryData.moda} />;
};

export default Moda;
